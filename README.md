# linkly-website
The official website for Linkly — handles the landing page, update checks, and APK distribution. Plain HTML, Tailwind, and a JSON file doing the job of a backend. No framework, no build step, no server. Just files that work and an app that updates itself. Stars optional, APK is free.
